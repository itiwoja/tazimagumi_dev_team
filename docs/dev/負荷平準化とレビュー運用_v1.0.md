# 負荷平準化とレビュー運用 v1.0

**プロジェクト：** 田島組 卒業制作（男の身だしなみアプリ）
**関連：** Issue #112 #30 #31 #54 #73 #74 #81 #109 ／[ブランチ運用ルール](./ブランチ運用ルール_v1.0.md)
**ステータス：** 提案（チーム合意待ち）

> チームのスループット上限が「特定個人の作業速度」で決まる構造を解消するための提案。
> データ整備の一極集中と、村上さんへのレビュー・アサイン集中の2つのボトルネックを平準化する。

---

## 現状の負荷（オープンIssueの担当別・2026-07-10 時点）

| 担当 | オープンIssue数 | 備考 |
| --- | --- | --- |
| 村上壱基 (`itiwoja`) | 23 | ＋全PRレビュー＋共有ファイル管理が集中 |
| 仲程天飛 (`takato9310`) | 20 | **データ整備が集中**（#30 #31 #32 #54 #47 ほか） |
| 田島優人 (`yourenputianjian-sketch`) | 14 | |
| 饒波廣翔 (`kuro1020`) | 13 | |
| 新田漣 (`ren-1222`) | 12 | |
| （`ikki-claude`） | 7 | 自律実装エージェントへ肩代わり中（本実験） |

> 未アサインのオープンIssueあり（2件）。着手前にアサインを付ける。

2つのボトルネック:
1. **データ整備が仲程さん1人に集中**。商品15〜20件の事実情報（成分・価格・容量）を薬機法に配慮して正確に集める作業はコードより時間がかかりうる。ここが遅れると推薦(#37)→SC-03(#38)が連鎖で止まる。
2. **村上さんにアサイン＋全PRレビュー＋共有ファイル管理が集中**。チームのスループット上限が村上さんのレビュー速度で決まる構造。

---

## 提案1: データ整備の分担

- **#30（商品マスター）をカテゴリ単位で分割**し、調査だけなら誰でもできる形にする
  （例: 洗顔5件を新田さん、化粧水5件を田島さん…のようにテンプレCSVを埋める作業として配る）。
- **入力フォーマットを先に固定**する。本リポジトリに [`docs/data/商品マスタ入力テンプレ.csv`](../data/商品マスタ入力テンプレ.csv) を用意した。
  - 列は `app/data/products.js` の Product 型と一致: `id,category,name,price,volume,budget,feel,scent,ingredients`。
  - `budget` は `core`（〜5,000円）/ `sub`（〜1,500円）のみ。
  - `ingredients` は**セミコロン `;` 区切り**で複数記入（例: `セラミド配合;無香料`）。「配合の有無」の事実のみ。効能の断定表現は禁止（薬機法）。
- **検証と変換を前倒し**すると複数人入力でも品質が揃う:
  - #73 バリデーション（`scripts/validate-products.mjs`・本リポジトリにPR済み）で必須項目・薬機法表現を機械チェック。
  - #74 CSV→JS変換で `products.js` へ反映（`ingredients` の `;` 区切りを配列に展開）。
- **最終チェック（成分タグの正確性・薬機法表現）だけ仲程さんに集約**する。調査・一次入力は分散。

## 提案2: レビュー負荷の軽減

- **#81 PRチェックCI を最優先で入れる**（本リポジトリにPR済み）。機械で見られるもの
  （JS構文・データ検証・SWキャッシュ更新もれ・ブランチ名）は機械に任せる。
- **独立画面のPRは相互レビューでマージ可**にする:
  - 共有ファイル（`app/css/style.css`・`app/js/state.js`・`app/js/main.js`）に**触れていない**PRは、担当者以外の誰か1人のレビューでマージしてよい。
  - **村上さんの必須レビューは、共有ファイルを触るPRだけ**に絞る。
  - これによりSC-02〜04のUIなど独立PRが村上さん待ちで詰まらなくなる。

---

## 完了条件（この提案の導入）

- [ ] データ整備の**分担表**が決まり、各Issueのアサインに反映される（※人間が決定）
- [ ] レビュールール変更がチーム合意され、`CLAUDE.md` / `AGENTS.md` に反映される（※人間が合意後に反映）

### 合意後に CLAUDE.md / AGENTS.md へ追記する文面（両ファイル同一で）

```markdown
## レビュー運用（負荷平準化）

- 共有ファイル（app/css/style.css・app/js/state.js・app/js/main.js）に触れないPRは、担当者以外の1人のレビューでマージ可。
- 村上（itiwoja）の必須レビューは、共有ファイルを触るPRのみ。
- 機械で見られる項目（JS構文・データ検証・ブランチ名・SWキャッシュ更新もれ）はCI（#81）に任せる。
```

---

## 改訂履歴

| 日付 | 版 | 変更点 |
| --- | --- | --- |
| 2026/07/10 | v1.0 | 初版（提案）。負荷可視化・データ分担・レビュー運用・CSV入力テンプレを定義（Issue #112） |
