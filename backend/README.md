# backend

ここに **Java + Spring Boot** のプロジェクトを配置します。

セットアップは別タスクで行います。それまでは、このディレクトリは存在を保つためのプレースホルダです。

## 予定

- Spring Boot で REST API を実装
- DB（MySQL or PostgreSQL）と連携
- フロントエンド (`/frontend`) からの呼び出しに応答する
