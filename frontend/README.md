# frontend

ここに **React (Vite + TypeScript)** のプロジェクトを配置します。

セットアップは別タスクで行います。それまでは、このディレクトリは存在を保つためのプレースホルダです。

## 予定

- React + Vite + TypeScript で初期化
- UIライブラリ（shadcn/ui など）を導入
- バックエンド (`/backend`) の REST API を呼び出す
